# Media — drop zone

Drop demo files here and I'll embed them into the parent project README at the right place.

## What to drop, where

| Filename | Goes into README section | Recommended format |
|---|---|---|
| `architecture.png` | Architecture section (replaces Mermaid render) | PNG, ≤ 1600 px wide |
| `demo-procedural-recoil.gif` | "The procedural animation pattern" section | GIF, high fps recommended |
| `demo-optic-pip.gif` | "Modular attachments" section | GIF, 600–1000 px wide |
| `demo-attachment-swap.gif` | "Modular attachments" section | GIF or MP4 |
| `demo-tactical-reload.gif` | "Custom extension modules" section | GIF, slow-mo recommended |
| `demo-multiplayer.mp4` | Hero / top section | MP4, ≤ 30 sec, shows replication |

## Capture tips

- The picture-in-picture optic is SKG's headline feature — capture it at full quality (1080p MP4) for the hero shot.
- Procedural recoil reads best when contrasted side-by-side with hand-keyed: consider a split-screen clip.
- For the multiplayer clip, show both clients' perspectives synchronized — that's the architecture story.

Once dropped, ping me and I'll wire each file into the parent README.
